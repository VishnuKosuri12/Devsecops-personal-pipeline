apiVersion: apps/v1
kind: Deployment
metadata:
  name: tic-tac-toe
  labels:
    app: tic-tac-toe
spec:
  # Maintain 4 running instances of the application
  replicas: 4 
  selector:
    matchLabels:
      app: tic-tac-toe
  template:
    metadata:
      labels:
        app: tic-tac-toe
    spec:
      containers:
        - name: tic-tac-toe
          image: ghcr.io/vishnukosuri12/devsecops-demo:sha-fb64dc2ac8cd8d79ad4a75c58326596b59eaf402
          # Ensure the cluster always pulls the specified image (useful for dev/staging, consider IfNotPresent for production)
          imagePullPolicy: Always
          # --- The original file was cut off here. Assuming it might have defined ports or resource limits.
          # Here is an example of what might follow, which you can uncomment or adjust:
          # ports:
          #   - containerPort: 8080
          # resources:
          #   limits:
          #     cpu: "500m"
          #     memory: "512Mi"
          #   requests:
          #     cpu: "250m"
          #     memory: "256Mi"
